# Deployment

## Steps
1) Prepare host (Docker, firewall rules)
2) Configure `.env`
3) `docker compose up -d`
4) Enroll agents
5) Validate dashboards

> Note: adjust images/versions to your target environment.
